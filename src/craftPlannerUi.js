import {
  createCraftProject,
  deleteCraftProject,
  loadCraftWorkspace,
  setCraftInventoryQuantity,
  updateCraftProject,
} from './craftWorkspace.js';
import { getInterludeItemIcon, installIconWithFallback } from './craftItemIcons.js';

const escapeHtml = (value = '') => String(value)
  .replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;')
  .replaceAll('"', '&quot;').replaceAll("'", '&#039;');
const fmt = value => Number(value || 0).toLocaleString('pl-PL');

function projectStatusLabel(status) {
  return ({ active:'AKTYWNY', paused:'WSTRZYMANY', completed:'ZAKOŃCZONY', archived:'ARCHIWUM' })[status] || String(status || '').toUpperCase();
}

function itemIconMarkup(itemKey, name, gameItemId = null) {
  return `<span class="craft-item-icon" data-craft-icon data-item-key="${escapeHtml(itemKey)}" data-item-name="${escapeHtml(name)}" data-game-item-id="${gameItemId || ''}" aria-hidden="true">⚒</span>`;
}

async function resolveIcon(node) {
  const { candidates } = await getInterludeItemIcon(node.dataset.gameItemId, node.dataset.itemName || '');
  installIconWithFallback(node, candidates, '⚒');
}

function hydrateIcons(root) { root.querySelectorAll('[data-craft-icon]').forEach(resolveIcon); }
function findItem(workspace, key) { return workspace?.items?.find(item => item.item_key === key) || null; }

function renderRequirementRows(project, workspace) {
  const rows = new Map(project.requirements.map(row => [row.itemKey, row]));
  for (const row of project.missing) if (!rows.has(row.itemKey)) rows.set(row.itemKey, { ...row, ownedAllocated:0, generatedSurplusUsed:0, missing:row.quantity });
  if (!rows.size) return '<p class="craft-muted">Brak składników do pokazania.</p>';
  return [...rows.values()].map(row => {
    const owned=Number(row.ownedAllocated||0), virtual=Number(row.generatedSurplusUsed||0), missing=Number(row.missing||0), needed=Number(row.quantity||0), covered=Math.max(0,needed-missing);
    const item=findItem(workspace,row.itemKey);
    return `<div class="craft-mat-row"><div class="craft-item-name">${itemIconMarkup(row.itemKey,row.name,item?.game_item_id)}<span><b>${escapeHtml(row.name)}</b><small>${escapeHtml(row.itemKey)}</small></span></div><span><em>Potrzeba</em><strong>${fmt(needed)}</strong></span><span><em>Pokryte</em><strong>${fmt(covered)}</strong></span><span><em>Magazyn</em><strong>${fmt(owned)}</strong></span><span><em>Nadwyżka</em><strong>${fmt(virtual)}</strong></span><span class="${missing?'craft-missing':'craft-ok'}"><em>Brakuje</em><strong>${fmt(missing)}</strong></span></div>`;
  }).join('');
}

function renderProjectCard(project,workspace) {
  const missingTotal=project.missing.reduce((s,r)=>s+Number(r.quantity||0),0), target=findItem(workspace,project.targetItemKey);
  return `<article class="craft-project-card" data-craft-project="${escapeHtml(project.id)}"><div class="craft-project-head"><div><small>${escapeHtml(projectStatusLabel(project.status))} · PRIORYTET ${fmt(project.priority)}</small><div class="craft-target-title">${itemIconMarkup(project.targetItemKey,project.targetName,target?.game_item_id)}<div><h4>${escapeHtml(project.name)}</h4><p>${escapeHtml(project.targetName)} × ${fmt(project.targetQuantity)}</p></div></div></div><div class="craft-project-actions">${project.status==='active'?`<button type="button" data-craft-action="pause" data-id="${escapeHtml(project.id)}">Wstrzymaj</button>`:`<button type="button" data-craft-action="resume" data-id="${escapeHtml(project.id)}">Wznów</button>`}<button type="button" data-craft-action="delete" data-id="${escapeHtml(project.id)}">Usuń</button></div></div><div class="craft-project-summary ${missingTotal?'has-missing':'is-complete'}"><b>${missingTotal?`Brakuje łącznie: ${fmt(missingTotal)}`:'Materiały pokryte ✓'}</b><span>${project.reservesInventory?'Projekt rezerwuje materiały z magazynu.':'Projekt nie rezerwuje materiałów.'}</span></div><div class="craft-mat-list">${renderRequirementRows(project,workspace)}</div></article>`;
}

function renderInventoryRows(workspace) {
  if(!workspace.plan.inventory.length)return '<p class="craft-muted">Magazyn jest pusty. Dodaj pierwszy materiał poniżej.</p>';
  return workspace.plan.inventory.map(row=>{const item=findItem(workspace,row.itemKey);return `<div class="craft-inventory-row"><div class="craft-item-name">${itemIconMarkup(row.itemKey,row.name,item?.game_item_id)}<span><b>${escapeHtml(row.name)}</b><small>${escapeHtml(row.itemKey)}</small></span></div><span><em>Mam</em><strong>${fmt(row.quantity)}</strong></span><span><em>Zarezerwowane</em><strong>${fmt(row.reserved)}</strong></span><span><em>Dostępne</em><strong>${fmt(row.available)}</strong></span><button type="button" data-craft-edit-stock="${escapeHtml(row.itemKey)}" data-current="${row.quantity}">Zmień</button></div>`}).join('');
}
function targetOptions(w){const keys=new Set(w.recipes.filter(r=>r.active!==false).map(r=>r.output_item_key));return w.items.filter(i=>keys.has(i.item_key)).map(i=>`<option value="${escapeHtml(i.item_key)}">${escapeHtml(i.name)}</option>`).join('')}
function allItemOptions(w){return w.items.map(i=>`<option value="${escapeHtml(i.item_key)}">${escapeHtml(i.name)}</option>`).join('')}

function ensureUiShell(){const layer=document.querySelector('#memberZoneLayer'),side=layer?.querySelector('.member-zone-side'),main=layer?.querySelector('.member-zone-main');if(!layer||!side||!main)return null;if(!side.querySelector('[data-zone-view="craft"]')){const b=document.createElement('button');b.type='button';b.className='zone-nav';b.dataset.zoneView='craft';b.innerHTML='⚒ <span>Craft</span>';side.insertBefore(b,side.querySelector('.zone-side-spacer')||null)}let panel=main.querySelector('[data-zone-panel="craft"]');if(!panel){panel=document.createElement('section');panel.className='zone-view';panel.dataset.zonePanel='craft';panel.innerHTML='<div class="zone-section-head"><small>OSOBISTY PLANER</small><h3>CRAFT WORKSPACE</h3><p>Twój magazyn, wiele projektów i automatyczne liczenie braków.</p></div><div id="craftWorkspaceRoot" class="craft-workspace"><p class="craft-muted">Otwórz zakładkę Craft, aby wczytać dane.</p></div>';main.appendChild(panel)}if(!document.querySelector('#craftPlannerUiStyles')){const s=document.createElement('style');s.id='craftPlannerUiStyles';s.textContent=`.craft-workspace{display:grid;gap:14px;padding-bottom:30px}.craft-toolbar{display:grid;grid-template-columns:1fr 1fr;gap:14px}.craft-box,.craft-project-card{border:1px solid #3d3220;background:#0a0e0e;padding:16px}.craft-box h4,.craft-project-card h4{margin:3px 0 5px;color:#ead9b3;font:700 18px Georgia}.craft-box>p,.craft-project-card p,.craft-muted{color:#837e75;font-size:11px;line-height:1.5}.craft-form{display:grid;grid-template-columns:1fr 110px auto;gap:8px;margin-top:12px}.craft-form.project{grid-template-columns:1.4fr 1fr 90px 90px auto}.craft-form input,.craft-form select{min-width:0;padding:10px;border:1px solid #4a3c25;background:#070b0b;color:#ddd}.craft-form button,.craft-project-actions button,.craft-inventory-row button{padding:9px 11px;border:1px solid #765925;background:#18130b;color:#e5bd65;font-weight:800;cursor:pointer}.craft-feedback{min-height:18px;margin:8px 0 0;color:#d9b45e;font-size:11px}.craft-projects{display:grid;gap:12px}.craft-project-head{display:flex;gap:12px;align-items:flex-start;justify-content:space-between}.craft-project-head small{color:#b98a39;font-size:9px;font-weight:900}.craft-project-actions{display:flex;gap:6px}.craft-project-summary{display:flex;justify-content:space-between;gap:10px;margin:12px 0;padding:10px;border:1px solid #3a3020;font-size:11px}.craft-project-summary.has-missing b{color:#df9a65}.craft-project-summary.is-complete b{color:#75c88a}.craft-project-summary span{color:#777269}.craft-mat-list,.craft-inventory-list{display:grid}.craft-mat-row,.craft-inventory-row{display:grid;grid-template-columns:minmax(190px,1.6fr) repeat(5,minmax(80px,.7fr));gap:8px;align-items:center;padding:10px 0;border-top:1px solid #292319}.craft-inventory-row{grid-template-columns:minmax(190px,1.6fr) repeat(3,minmax(90px,.7fr)) auto}.craft-item-name,.craft-target-title{display:flex;align-items:center;gap:9px}.craft-item-icon{width:32px;height:32px;flex:0 0 32px;display:grid;place-items:center;border:1px solid #514225;background:#11100c;color:#7f6c43;font-size:14px;overflow:hidden}.craft-item-icon img{width:32px;height:32px;object-fit:contain;image-rendering:auto}.craft-target-title{margin-top:4px}.craft-target-title .craft-item-icon{width:40px;height:40px;flex-basis:40px}.craft-target-title .craft-item-icon img{width:40px;height:40px}.craft-mat-row b,.craft-inventory-row b{display:block;color:#d8d2c8;font-size:12px}.craft-mat-row small,.craft-inventory-row small{display:block;color:#666159;font-size:9px}.craft-mat-row span,.craft-inventory-row span{display:grid}.craft-mat-row em,.craft-inventory-row em{color:#6f6a62;font-size:8px;font-style:normal;text-transform:uppercase}.craft-mat-row strong,.craft-inventory-row strong{color:#c9c2b5;font-size:12px}.craft-missing strong{color:#dd8a58}.craft-ok strong{color:#6fc184}.craft-section-title{display:flex;align-items:end;justify-content:space-between;margin-bottom:8px}.craft-section-title small{color:#9b824f}.craft-empty,.craft-loading{padding:26px;text-align:center;color:#82765f}.craft-empty{border:1px dashed #514225}.craft-workspace [disabled]{opacity:.5;cursor:not-allowed}@media(max-width:900px){.member-zone-side{grid-template-columns:repeat(5,minmax(0,1fr))!important}.craft-toolbar{grid-template-columns:1fr}.craft-form,.craft-form.project{grid-template-columns:1fr 1fr}.craft-form button{grid-column:1/-1}.craft-project-head,.craft-project-summary{display:block}.craft-project-actions{margin-top:10px}.craft-mat-row{grid-template-columns:1fr 1fr 1fr}.craft-mat-row>div{grid-column:1/-1}.craft-inventory-row{grid-template-columns:1fr 1fr 1fr}.craft-inventory-row>div{grid-column:1/-1}.craft-inventory-row button{grid-column:1/-1}}`;document.head.appendChild(s)}return{layer,side,main,panel,root:panel.querySelector('#craftWorkspaceRoot')}}

export function installCraftPlannerUi(supabase){if(!supabase||document.documentElement.dataset.craftPlannerInstalled==='1')return;const ui=ensureUiShell();if(!ui)return;document.documentElement.dataset.craftPlannerInstalled='1';let workspace=null,loading=false;const setFeedback=m=>{const n=ui.root.querySelector('.craft-feedback');if(n)n.textContent=m||''};const refresh=async({quiet=false}={})=>{if(loading)return;loading=true;if(!quiet)ui.root.innerHTML='<div class="craft-loading">Ładowanie Twojego craft workspace…</div>';try{workspace=await loadCraftWorkspace(supabase);const projects=workspace.plan.projects.slice().sort((a,b)=>a.priority-b.priority).map(p=>renderProjectCard(p,workspace)).join('');ui.root.innerHTML=`<div class="craft-toolbar"><section class="craft-box"><div class="craft-section-title"><div><small>NOWY CEL</small><h4>Dodaj projekt craftu</h4></div></div><p>Każdy aktywny projekt automatycznie rezerwuje potrzebne materiały zgodnie z priorytetem.</p><form class="craft-form project" id="craftProjectForm"><input name="name" maxlength="80" required placeholder="np. 3× Draconic Bow"><select name="targetItemKey" required>${targetOptions(workspace)}</select><input name="targetQuantity" type="number" min="1" step="1" value="1" required><input name="priority" type="number" min="0" step="10" value="100" required><button type="submit">Dodaj</button></form></section><section class="craft-box"><div class="craft-section-title"><div><small>TWÓJ STAN</small><h4>Dodaj / zaktualizuj magazyn</h4></div></div><p>Wpisujesz realny stan. Projekty nie zmieniają go fizycznie — tylko pokazują, ile jest zarezerwowane.</p><form class="craft-form" id="craftStockForm"><select name="itemKey" required>${allItemOptions(workspace)}</select><input name="quantity" type="number" min="0" step="1" value="0" required><button type="submit">Zapisz</button></form></section></div><p class="craft-feedback"></p><section class="craft-box"><div class="craft-section-title"><div><small>MAGAZYN</small><h4>Mój stan materiałów</h4></div><small>Mam · Zarezerwowane · Dostępne</small></div><div class="craft-inventory-list">${renderInventoryRows(workspace)}</div></section><section><div class="craft-section-title"><div><small>PROJEKTY</small><h4>Moje craft projekty</h4></div><small>${workspace.plan.projects.length} projektów</small></div><div class="craft-projects">${projects||'<div class="craft-empty">Nie masz jeszcze żadnego projektu craftu.</div>'}</div></section>`;hydrateIcons(ui.root)}catch(e){ui.root.innerHTML=`<div class="craft-empty"><b>Nie udało się wczytać planera.</b><p>${escapeHtml(e?.message||e)}</p></div>`}finally{loading=false}};ui.side.addEventListener('click',e=>{if(e.target.closest('[data-zone-view="craft"]'))setTimeout(()=>refresh(),0)});ui.root.addEventListener('submit',async e=>{e.preventDefault();const f=e.target,d=new FormData(f);try{if(f.id==='craftProjectForm'){await createCraftProject(supabase,{name:d.get('name'),targetItemKey:d.get('targetItemKey'),targetQuantity:Number(d.get('targetQuantity')),priority:Number(d.get('priority')),reservesInventory:true});await refresh()}else if(f.id==='craftStockForm'){await setCraftInventoryQuantity(supabase,d.get('itemKey'),Number(d.get('quantity')));await refresh()}}catch(err){setFeedback(err?.message||String(err))}});ui.root.addEventListener('click',async e=>{const edit=e.target.closest('[data-craft-edit-stock]');if(edit){const q=prompt('Ile sztuk realnie masz?',edit.dataset.current||'0');if(q===null)return;const n=Number(q);if(!Number.isFinite(n)||n<0)return setFeedback('Podaj poprawną ilość.');await setCraftInventoryQuantity(supabase,edit.dataset.craftEditStock,n);return refresh()}const a=e.target.closest('[data-craft-action]');if(!a)return;try{if(a.dataset.craftAction==='delete'){if(!confirm('Usunąć ten projekt craftu?'))return;await deleteCraftProject(supabase,a.dataset.id)}else await updateCraftProject(supabase,a.dataset.id,{status:a.dataset.craftAction==='pause'?'paused':'active'});await refresh()}catch(err){setFeedback(err?.message||String(err))}});return{refresh}}
